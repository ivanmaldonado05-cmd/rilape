# Dependencias y artefactos de build (no se despliegan)
node_modules/
package-lock.json

# Material de extracción del PDF (pesado; ya convertido a /assets/img)
_extract/

# Archivos del sistema
Thumbs.db
.DS_Store
*.log
